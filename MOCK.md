# 목(Mock) 모드

**서버를 하나도 띄우지 않고 앱만 켜서 전 화면을 돌려보는 모드다.**
Spring(:8080) · OCR FastAPI(:8000) · Postgres · OpenAI 없이 로그인부터 스캔·저장·검색·챗봇까지 전부 동작한다.

## 언제 쓰나

- 백엔드 세팅 없이 **UI/인터랙션만** 만지거나 디버깅할 때
- 서버가 죽었거나 같은 Wi-Fi 가 아닐 때 (카페·이동 중)
- 스켈레톤·로딩·에러·빈 상태처럼 **실서버로는 만들기 번거로운 화면**을 볼 때
- 새 팀원이 클론하자마자 앱을 실행해야 할 때

실서버 모드가 **항상 기본값**이다. 아래 두 스위치 중 하나를 켜야만 목이 동작하고,
꺼져 있으면 네트워크 계층 코드 경로가 지금과 100% 같다.

## 켜는 법

### 1) `.env` — 빌드 타임 (목 전용 빌드를 만들 때)

```
EXPO_PUBLIC_MOCK=1
```

`.env` 를 고친 뒤에는 **Metro 를 재시작**해야 한다 (`EXPO_PUBLIC_*` 는 번들에 굽힌다).
기본값은 `EXPO_PUBLIC_MOCK=0`(실서버)이고 `.env.example` 에 주석과 함께 들어 있다.

### 2) 설정 화면 — 런타임 (이미 설치된 앱에서 켤 때)

```
설정 → 개발 → 목 모드 → 토글
```

- 개발 섹션은 **development / preview 빌드에서만** 보인다 (production 프로파일에서는 섹션 자체가 없다).
- 토글 값은 MMKV(`mock.enabled`)에 남고 **빌드 플래그보다 항상 우선**한다. 즉 목 빌드에서 목을 끌 수도 있다.
- ⚠️ **바꾼 뒤에는 앱을 완전히 종료했다가 다시 실행해야 한다.** React Query 캐시(gcTime 30분)에
  이전 모드의 응답이 남아 있어서, 토글만 뒤집으면 화면이 옛 데이터를 그대로 보여 준다.

같은 화면에서 현재 상태(빌드 플래그 / 런타임 토글 / 목 데이터 건수)를 확인하고,
**데이터 초기화**(씨드 + 스캔 순환 복원)와 **다음 스캔 결과 미리보기**도 할 수 있다.

## 로그인

아무 이메일이나 **8자 이상** 비밀번호면 통과한다 (예: `mock@mora.dev` / `mora1234!`).
비밀번호가 8자 미만이면 400 을 돌려주므로 **로그인 에러 화면도 확인할 수 있다.**
로그인한 이메일이 목 계정에 그대로 반영되어 프로필 화면과 어긋나지 않는다.

소셜 로그인(구글/카카오/네이버)은 목이 없다 — 인가 페이지가 실제 Spring 서버에 있기 때문이다.

## 목 데이터에 뭐가 들어있나

앱을 처음 켤 때 씨드 1벌이 만들어져 MMKV(`mock.db`)에 저장된다.
저장·수정·삭제가 **다음 실행에도 남는다** — 껐다 켜면 사라지는 데이터로는 디자인 검증이 안 되기 때문이다.

| 항목 | 수량 | 비고 |
| --- | --- | --- |
| 명함 | 8건 | 그룹 3개(협력사·컨퍼런스·고객사)에 나눠 담김 + 미분류 |
| 포스터 | 4건 | 2건이 **마감 임박**(오늘+3일 / 오늘+7일) → 홈 캐러셀·알림에 뜬다 |
| 티켓 | 3건 | 1건이 **오늘 출발** → 홈 `오늘 일정`에 뜬다 |
| 영수증 | 3건 | 품목 8개 포함 |
| 알림 | 6건 | 읽음/안읽음 섞임 → 배지 숫자 확인 가능 |
| 최근 검색어 | 5건 | |
| 구글 캘린더 | 연동됨 | `mora.mock@gmail.com` |

**날짜는 전부 오늘 기준 상대값이다.** 손대지 않은 씨드는 날짜가 바뀌면 자동으로 다시 만들어진다
(안 그러면 며칠 뒤 `마감 임박`도 `오늘 일정`도 비어 버린다). 한 번이라도 데이터를 고치면 보존한다.

### 스캔은 무엇을 내놓나

문서 유형이 **랜덤이 아니라 순환**한다: 명함 → 포스터 → 영수증 → 티켓.
원하는 유형을 최대 4번 안에 반드시 다시 만날 수 있어야 디버깅이 되기 때문이다.
**3번에 1번은 저신뢰(< 0.55)** 로 나와서 종류 선택 시트(CLS-03) 경로가 저절로 검증된다.
다음에 뭐가 나올지는 설정 → 개발 → 목 모드 화면에서 미리 볼 수 있다.

업로드 진행률 · 취소 버튼 · 서버 처리 구간(인디터미네이트) 전환도 그대로 재현된다.

## 한계 (반드시 알고 쓸 것)

- **실제 OCR 이 없다.** 찍은 사진과 무관하게 미리 준비된 샘플 텍스트가 나온다.
  카메라·크롭·압축까지는 진짜로 돌지만 인식 결과는 사진과 아무 상관이 없다.
- **이미지가 없다.** 목에는 파일 시스템이 없어 `image_url` 이 항상 빈 문자열이다.
  → 저장 흐름이 R2 경로를 타서 완료 화면에 `이미지 없이 저장됨` 배너가 뜬다. 이게 정상이다.
  → 보관함·상세 화면의 썸네일은 전부 플레이스홀더다. 이미지 뷰어는 검증할 수 없다.
- **임베딩이 없다.** 검색·챗봇의 유사도는 벡터가 아니라 **문자열 부분일치 점수**다
  (실서버 하이브리드 점수와 같은 0.6~0.98 대역으로 사상했다). 의미 검색("작년 출장 영수증")은 안 되고
  키워드가 실제로 들어 있어야 걸린다. 대신 **결정적**이라 같은 질의는 항상 같은 순서를 낸다.
- **챗봇 답변이 LLM 이 아니다.** 검색 결과를 템플릿에 끼워 만든 문장이다.
- **소셜 로그인 불가.** OAuth 인가 페이지는 실제 서버에 있다.
- **구글 캘린더 연결 URL 은 400 이다.** 실서버도 client-id 미설정으로 400 이라 그대로 재현했다
  (가짜 URL 을 주면 인앱 브라우저가 착지하지 못하고 90초 매달린다). 연동 **해제**는 동작한다.
- **응답이 항상 200~600ms 걸린다.** 일부러 넣은 지연이다 — 즉시 응답하면 스켈레톤·로딩·취소 버튼을
  볼 수 없어 목 모드의 목적이 절반 사라진다.
- 서버 연결 진단(설정 → 개발 → 서버 연결 진단)은 목 모드에서 `목 모드 — 서버에 연결하지 않고…`
  로 표시된다. 실서버 정상(`정상 응답`)과 문구로 구분된다.

## 실서버로 돌아가는 법

1. 설정 → 개발 → 목 모드 → **목 모드 끄기**.
2. `.env` 에 `EXPO_PUBLIC_MOCK=0` 인지 확인한다.
3. **앱을 완전히 종료했다가 다시 실행한다** (React Query 캐시 때문).
4. `EXPO_PUBLIC_API_URL` / `EXPO_PUBLIC_OCR_URL` 이 서버 주소를 가리키는지 확인하고,
   설정 → 개발 → 서버 연결 진단으로 두 서버가 `정상 응답` 인지 본다.

목 데이터(`mock.db`)는 지워지지 않고 남는다 — 다음에 목 모드를 켜면 그대로 이어진다.
완전히 초기화하려면 설정 → 개발 → 목 모드 → **데이터 초기화**.

## 구조 (고칠 사람만)

목은 **네트워크 계층에만** 주입한다. 화면 코드는 목의 존재를 모른다.

```
src/mocks/
  config.ts    플래그 판정 (isMockEnabled) — 모든 주입 지점의 유일한 판정 함수
  handlers.ts  53개 라우트 테이블 (method, path) → 응답. 지연 200~600ms
  db.ts        인메모리 + MMKV(mock.db) 저장소, 평면 Page 조립, 문자열 매칭 점수
  fixtures.ts  씨드 데이터
  chat.ts      챗봇 답변 생성
  scan.ts      /api/scan · /api/commit (XHR 업로더라 http 계층으로 못 가로챈다)
  health.ts    진단 화면 프로브
```

주입 지점은 **3곳뿐이다.**

| 파일 | 주입 |
| --- | --- |
| `src/services/http.ts` | `request()` 첫 줄 — 앱의 52개 API 호출이 전부 여기로 모인다 |
| `src/features/scan/api.ts` | `scanImage()` · `commitDocument()` — 진행률 때문에 XHR 을 써서 별도 |
| `src/services/health.ts` | `probeSpring()` · `probeOcr()` — 직접 `fetch` |

응답 모양은 실서버와 **글자 단위로 같아야 한다**: `{success, data, error?, message?}` 봉투,
목록은 평면 `Page`, `/api/card-groups` 와 `/api/*/search` 는 `data` 가 바로 배열,
없는 id 는 404 가 아니라 **500**, `unread-count` 는 `{count:N}`.
정본은 `wiki/tech/API Contract.md` 와 각 `src/features/*/api.ts` 의 파서다.
